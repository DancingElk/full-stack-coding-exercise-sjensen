{
  "name": "full-stack-coding-exercise-client",
  "version": "0.1.0",
  "proxy": "http://localhost:3011",
  "private": true,
  "dependencies": {
    "lodash": "^4.17.21",
    "nodemon": "^2.0.15",
    "react": "17.0.2",
    "react-dom": "17.0.2",
    "react-scripts": "5.0.0"
  },
  "scripts": {
    "start": "npx concurrently \"node server/index.js\" \"PORT=3010 react-scripts start\"",
    "start-client": "react-scripts start",
    "build": "react-scripts build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
